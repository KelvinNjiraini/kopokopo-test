In order to test out the working of the code, plug in the arguments in the parameters of the final
"qualifiedClients" function at the bottom and once done, run the command "node main.js".

Remember to put the first parameter of the function as a string and the second parameter as a number.